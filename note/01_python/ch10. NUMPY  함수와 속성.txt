numpy 패키지의 함수와 속성
- 배열속성
  dtype: 요소들의 타입(타입변경시 astype(타입)
  itemsize: 요소들 배열의 갯수
  ndim: 차원수
  SIZE: 요소 갯수
  SHPE: 배열 구조
- 배열 생성
  np.array(obj, dtype, copy=T/F)
  np.zeros(tup.dup.dtype)
  np.ones(tup.dtype)
  np.full
 np.random.random(tup):
 np.random.uniform
np.random.normal: 평균loc와 표준편차 scale을 갖는 랜덤값
np.random.rand(n): 균등분포의 1차원 난수 배열, 0부터 1미만의 균등분포의 난수 배열 발생(난수 n개)
np.random.randn(n): 표준정규분포의 1차원 난수 배열(평균0, 표준편차1)
np.arage(from, to, by, dtype)
np.linspace((from, to, num):

- 배열 차원 변경
reshape():-1사용하면 자동계산
resize(): -1불가
T, transpose(): 전치행렬
ravel(), flatten(): 1차원 변경
